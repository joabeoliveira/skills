# Claude Code Instructions

Instruções específicas para Claude Code (e Claude.app) em projetos HTML/CSS/JavaScript.

## 🎯 Objetivo

Fornecer contexto ao Claude para gerar código profissional em HTML5, CSS3, JavaScript ES6+ e Tailwind CSS com foco em acessibilidade, performance e portabilidade.

## 📋 Diretrizes

### Linguagem
- **Comunicação**: Português (Brasil).
- **Código**: Inglês (nomes, funções, variáveis).
- **Comentários**: Português (explicações técnicas).

### Stack Tecnológico
- **Frontend**: HTML5 semântico + CSS3 + JavaScript ES6+
- **Styling**: Tailwind CSS (mobile-first, dark mode)
- **JavaScript**: Vanilla (sem frameworks) ou bibliotecas leves
- **Gráficos**: Chart.js, D3.js, SVG puro
- **Compatibilidade**: Browsers modernos (Chrome, Firefox, Safari, Edge últimas 2 versões)

### Princípios de Design
1. **Mobile-First**: Comece com mobile, scale para desktop
2. **Dark Mode**: Suporte nativo com Tailwind `dark:` classes
3. **Acessibilidade**: WCAG 2.1 AA obrigatório
4. **Performance**: LCP < 2.5s, CLS < 0.1
5. **SEO**: Meta tags, estrutura semântica, Open Graph

### Qualidade de Código
- JavaScript modular (ES6 `import/export`)
- TypeScript opcional mas recomendado
- Sem `var`; use `const`/`let`
- Sem CSS inline; use classes Tailwind
- Validação de entrada obrigatória
- Tratamento de erros explícito
- DRY: Don't Repeat Yourself

### Acessibilidade (Requisito)
✅ **Obrigatório**:
- HTML semântico: `<header>`, `<nav>`, `<main>`, `<article>`, `<footer>`
- Labels + inputs: `<label for="id"><input id="id">`
- Alt em imagens: `alt="descrição significativa"`
- Contraste: 4.5:1 texto normal, 3:1 texto grande
- Navegação por teclado: Tab, Enter, Esc, Setas
- Foco visível: `focus:outline-2 focus:ring-blue-500`
- Aria: `aria-label`, `aria-describedby`, `role` quando necessário
- Respeite `prefers-reduced-motion`

### Performance
✅ **Checklist**:
- Imagens: `loading="lazy"`, WebP + fallback, `alt`
- Scripts: `<script defer>` não-críticos
- CSS: Inline crítico, link resto
- Bundle JS < 100KB primeira página
- Promise.all() requisições paralelas
- Debounce/throttle para eventos frequentes
- Lazy load modules com `import()`

### Dark Mode
- Use classes Tailwind: `dark:bg-gray-900`, `dark:text-white`
- Teste ambas paletas
- Ofereça toggle ao usuário
- Salve preferência em `localStorage`

## 🏗️ Estrutura Recomendada

```
projeto/
├── index.html
├── js/
│   ├── main.js (entrada)
│   ├── components/
│   │   ├── navbar.js
│   │   ├── modal.js
│   │   └── form-handler.js
│   └── utils/
│       ├── dom.js
│       ├── validation.js
│       ├── api.js
│       └── storage.js
├── css/
│   ├── style.css (Tailwind + custom)
│   └── animations.css (animações)
├── img/ (otimizadas)
├── assets/ (fonts, icons)
└── tailwind.config.js
```

## 🔍 Checklist de Qualidade

Antes de finalizar qualquer código:

```
HTML/Acessibilidade:
  ☐ Semântica correta (<header>, <nav>, <main>, <footer>)?
  ☐ Labels associados a inputs?
  ☐ Alt em todas imagens?
  ☐ Navegação por teclado funcional?
  ☐ Contraste >= 4.5:1?
  ☐ Teste com leitor de tela (NVDA, VoiceOver)?

CSS/Styling:
  ☐ Mobile-first (sm:, md:, lg: breakpoints)?
  ☐ Dark mode testado (dark: classes)?
  ☐ Tailwind utilizado (sem inline CSS)?
  ☐ Componentes reutilizáveis?

JavaScript:
  ☐ Modular (import/export)?
  ☐ Entrada validada?
  ☐ Erros tratados?
  ☐ Performance otimizada?
  ☐ Sem console logs em produção?

Performance:
  ☐ Imagens lazy loaded + otimizadas?
  ☐ Scripts defer/async apropriados?
  ☐ Bundle < 100KB?
  ☐ Web Vitals OK?
```

## 📚 Recursos & Skills

Consulte:
- `.skills/html-structure/` - Padrões HTML + SEO
- `.skills/tailwind-css/` - Styling com Tailwind
- `.skills/javascript-vanilla/` - JavaScript modular
- `.skills/graphics-animations/` - Gráficos + performance
- `.skills/accessibility-emag/` - Acessibilidade profunda
- `.skills/performance-optimization/` - Web Vitals + otimizações

## ⚡ Prototipagem Rápida

Para MVPs, foque em:
1. HTML semântico
2. Tailwind CSS (pré-built components)
3. JavaScript vanilla (sem frameworks)
4. Teste acessibilidade básica

Evitar sobre-engenharia até ter feedback do usuário.

## 🚀 Deployment

- Static hosting: Netlify, Vercel, GitHub Pages
- Minifique assets (Terser, cssnano)
- Cache headers: `max-age=31536000` para assets hashed
- Implemente Service Worker para PWA (opcional)

---

**Última atualização**: 23 de fevereiro de 2026
